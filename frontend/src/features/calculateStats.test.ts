import { describe, it, expect } from "vitest";
import { calculateLegsIntermediateStopsCount } from "./calculateStats";

describe("calculateLegsIntermediateStopsCount()", () => {
	it("", () => {
		expect(calculateLegsIntermediateStopsCount()).toBe());
	});
});